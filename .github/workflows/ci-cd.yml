name: NEPS Infrastructure CI

on:
  push:
    branches: [main, feature/infra-pipeline-v2]
  pull_request:
    branches: [main]

# Avoid duplicate runs for the same PR
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ─── CORE VALIDATION ───
  # We combine essential checks to reduce job count and overhead
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create CI secret stubs
        run: |
          mkdir -p secrets
          printf 'neps' > secrets/postgres_user.txt
          printf 'ci_password' > secrets/postgres_password.txt
          printf 'ci_secret_key_32chars_minimum____' > secrets/app_secret_key.txt
          printf 'ci_redcap_token' > secrets/redcap_api_token.txt
          printf 'https://discord.com/api/webhooks/0/placeholder' > secrets/discord_webhook_url.txt

      - name: Validate Docker Compose
        run: |
          docker compose -f docker-compose.yml config --quiet
          docker compose -f docker-compose.yml -f docker-compose.prod.yml config --quiet

      - name: Validate Nginx
        run: |
          mkdir -p nginx/ssl
          touch nginx/ssl/neps.crt nginx/ssl/neps.key
          # Pull image once to speed up
          docker pull nginx:alpine --quiet
          # Use default config structure but point to our file
          docker run --rm \
            -v $PWD/nginx/nginx.conf:/etc/nginx/nginx.conf:ro \
            -v $PWD/nginx/ssl:/etc/nginx/ssl:ro \
            nginx:alpine nginx -t -c /etc/nginx/nginx.conf

      - name: Check Scripts
        run: |
          for script in scripts/*.sh; do
            bash -n "$script"
            head -n1 "$script" | grep -qE '#!/bin/(bash|sh)' || (echo "Missing shebang in $script" && exit 1)
          done

  # ─── SECURITY ───
  security:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Secret Scan
        uses: trufflesecurity/trufflehog@main
        continue-on-error: true # Don't block dev if historical secrets exist
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --only-verified

      - name: Container Config Scan
        uses: aquasecurity/trivy-action@0.20.0
        with:
          scan-type: 'config'
          scan-ref: 'docker-compose.yml'
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'

      - name: Upload Results
        uses: github/codeql-action/upload-sarif@v3
        if: always() && hashFiles('trivy-results.sarif') != ''
        with:
          sarif_file: 'trivy-results.sarif'
